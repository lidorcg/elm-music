query allPlaylists {
  allPlaylists {
    id
    name
    tracks {
      name
      artists
      duration
      youtubeId
    }
  }
}

mutation createPlaylist($name: String!) {
  createPlaylist(name: $name) {
    ok
  }
}
