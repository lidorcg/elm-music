query playLists {
  playlists {
    id
    name
    tracks {
      id
      name
      duration
      artists
      youtubeId
    }
  }
}


mutation createPlaylist($name: String!) {
  createPlaylist(name: $name) {
    ok
  }
}
